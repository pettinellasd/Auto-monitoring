apiVersion: 1

providers:
  - name: 'local-json'
    orgId: 1
    folder: 'Auto'
    type: file
    disableDeletion: false
    allowUiUpdates: true
    updateIntervalSeconds: 10
    options:
      # ATTENZIONE: path visto DENTRO il container
      path: /var/lib/grafana/dashboards
